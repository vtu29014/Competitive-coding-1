PROGRAM:

#include <stdio.h>

#include <stdlib.h>

int main(){

int T, n, i, j, k, sum, size;

scanf("%d", &T);

while (T-) {

scanf("%d", &n);

size (n(n+1))/2;

int *arr = (int*) malloc(sizeof(int) * size);

for (i = 0; i < size; i++) {

{

scanf("%d", &arr[i]);

sum = 0:

k = 0;

for (i=1; i <= n; i++) { if (i % 2 != 0) {

}

for (j=0; j<i; j++) {

int max = j; maxi

for (int 1=j+1;1<i; I++) {

if (arr[1+k] > arr[max +k]) {

}

}

max = 1;

int temp = arr[j + k];

arr[j + k] = arr[max + k];

arr[max +k] = temp;

sum += arr[k];

k++;

}

else {

}

}

}

for (j=0; j<i;j++) { min;

for (int 1=j+1;1<i; I++) {

if (arr[1+k]<arr[min + k]) {

min = 1;

}

int temp = arr[j + k];

}

arr[j+k] = arr[min + k]; arr[min+k] = temp;

sum += arr[k];

k+= i;

printf("%d\n", sum);
